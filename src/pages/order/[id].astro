---
import TaxonPage from '../../components/TaxonPage.astro';
import { nodes, type WhaleNode } from '../../utils/whales';

export async function getStaticPaths() {
  return nodes
    .filter((node) => node.type === 'order')
    .map((taxon) => ({ params: { id: taxon.id }, props: { taxon } }));
}

const { taxon } = Astro.props as { taxon: WhaleNode };
---

<TaxonPage
  taxon={taxon}
  heading="All Species in this Order"
  title={`${taxon.label} Order`}
  description={`Explore all whale species in the ${taxon.label} order`}
/>
